# URL base da API do Scriptorium, sem barra final.
# Em dev local: aponta pro Scriptorium.API rodando na sua máquina.
# Em produção (Docker): definido via variável de ambiente de build no
# docker-compose.yml (ver serviço "oratorium").
VITE_API_BASE_URL=http://localhost:8110
