{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "ConnectionStrings": {
    "_comentario": "Caminho do arquivo SQLite. Em produção (Docker), este caminho aponta para dentro do volume montado — veja docker-compose.yml na raiz do monorepo. A API só LÊ deste banco; quem escreve é o processo Scriptorium.Worker.",
    "Default": "Data Source=/data/scriptorium.db"
  },

  "LmStudio": {
    "_comentario": "A API não traduz nada diretamente (só o Worker faz isso), mas registra o HttpClient de tradução por consistência de configuração entre os dois processos.",
    "BaseUrl": "http://localhost:1234",
    "ModelName": "local-model",
    "TimeoutSeconds": 120
  }
}
